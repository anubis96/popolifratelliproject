{% extends '/istm_index/books/base_books.html.twig' %}

{% block title %}
  Nos Réalisations - POPOLI FRATELLI - RDC
{% endblock %}

{% block body %}
  <div class="container">
    <div class="news-events-container">
      <div class="last-news">
        <h3>
          Nos realisations
        </h3>
        <p>
          PF - RDC, enregistrée officiellement en RD Congo, a accompli des progrès significatifs dans sa mission humanitaire. Découvrez comment nous avons impacté les communautés locales et répondu aux urgences avec efficacité.
        </p>
        <div class="flex gap-2 wrap">
          {% for book in studentsBooks|slice(-10)|reverse %}
          <div class="card" style="width: 22rem;">
            <img src="{{ book.fileUrl }}" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title">{{ book.title }}</h5>
              <p class="card-text">{{ book.studentFullName }}</p>
            </div>
            <ul class="list-group list-group-flush">
              <li class="list-group-item">Année {{ book.year | date("Y") }}</li>
              {# <li class="list-group-item"><a href="{{ path('app_student_book_show', {'id': student_book.id}) }}"></a> Voir plus {{ book.year | date("Y") }}</li> #}
            </ul>
          </div>
            {# <li class='book'>
              <div class="book-content flex">
              <p>{{ book.studentFullName }}</p>
              <h5>{{ book.title }}</h5>
              <div class="view_download flex">
                <a href="{{ book.fileUrl }}" target="_blank"><i title="Visionner ou Télécharger le document" class="fa-solid fa-file-arrow-down"></i></a>
              </div>
              <h6>Année {{ book.year | date("Y") }}</h6>
              </div>
            </li> #}
          {% endfor %}
        </div>
      </div>
      {# <div class="last-events">
        <h3>
          Derniers publications des professeurs
        </h3>
        <ul class='list-inline flex'>
        {% for book in studentsBooks|slice(-4)|reverse %}
          <li class="book professor">
            <div class="book-content flex">
            <p>{{ book.studentFullName }}</p>
            <h5>{{ book.title }}</h5>
            <div class="view_download flex">
              <a href="{{ book.fileUrl }}" target="_blank"><i title="Visionner ou Télécharger le document" class="fa-solid fa-file-arrow-down"></i></a>
            </div>
            <h6>Année {{ book.year | date("Y") }}</h6>
            </div>
          </li>
        {% endfor %}
        </ul>
      </div> #}
    </div>
  </div>
{% endblock %}
